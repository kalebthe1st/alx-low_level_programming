#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("%d",size(int));
size(char);
size(int);
size(long int);
size(long long int);
size(float);
return (0);
}
