#include <stdlib.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry
 *
 * Return: Always 0
 */
int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	{	putchar (ch);
		putchar ('\n');
		ch--;
}
	return (0);
}

